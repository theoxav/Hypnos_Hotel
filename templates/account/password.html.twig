{% extends 'layouts/base.html.twig' %}

{% block title %}Update password -
{% endblock %}

{% block body %}
	<h1 class='mt-5 text-info'>Modification du mot de passe</h1>
	<a href="{{path('app_account')}}">Retour à mon Compte</a>
	<hr>
	
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit" class="btn btn-dark rounded w-100">Mettre à jour</button>
	{{ form_start(form) }}
	{{ form_end(form) }}
{% endblock %}
